spring:
  application:
    name: second-service
  main:
    banner-mode: "off"
server:
  port: 8082
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,httptrace,prometheus